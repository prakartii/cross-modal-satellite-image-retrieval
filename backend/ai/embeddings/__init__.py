"""
AKSHA — Embeddings Subpackage

Modules:
  embeddingGenerator → Normalize 32-dim feature vector to unit embedding
  vectorStore        → In-memory archive of 50 synthetic satellite scene embeddings
  similarity         → Cosine similarity computation (batch + single)
"""
