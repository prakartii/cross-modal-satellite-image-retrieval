"""
AKSHA — API Subpackage

FastAPI route handlers:
  upload  → /api/upload   (POST): Accept image, return metadata + preview
  search  → /api/search   (GET):  Text-based archive search for AI Copilot
  analyze → /api/analyze  (POST): Full pipeline with Server-Sent Events streaming
"""
